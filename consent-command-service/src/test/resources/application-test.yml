spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/consent_test
        username: test
        password: test
    jpa:
        hibernate:
            ddl-auto: none
        show-sql: true
    flyway:
        enabled: true
        locations: classpath:db/migration
    kafka:
        bootstrap-servers: localhost:9092
